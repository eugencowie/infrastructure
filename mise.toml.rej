diff a/mise.toml b/mise.toml	(rejected hunks)
@@ -5,6 +5,10 @@ disable_backends = ["asdf"] # cannot be updated by Renovate
 pipx = "latest"
 "pipx:cruft" = "latest"
 
-[tasks]
-"template:update" = { run = "cruft update", description = "Update project with the latest changes from the template" }
-"template:restore" = { run = "cruft diff | git apply", description = "Restore template-managed files" }
+[tasks."template:update"]
+description = "Update project with the latest changes from the template"
+run = "cruft update"
+
+[tasks."template:restore"]
+description = "Restore template-managed files"
+run = "cruft diff | git apply"
